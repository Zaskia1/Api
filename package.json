{
  "name": "backend-express-api",
  "version": "1.0.0",
  "description": "Simple CRUD API for customers using Express (in-memory + data.json persistence).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
